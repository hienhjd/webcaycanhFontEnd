import { GetApi } from "./ApiConfig"
// import { product, setproduct } from "./Config";

// export async function ApiGetDat() {
//     try {
//       const data = await GetApi({ path: "/product/getAll" }); // Gọi API và đợi dữ liệu
//       console.log(data);
      
//       const productData = JSON.stringify(data); // Chuyển dữ liệu thành chuỗi JSON
  
//       setproduct(productData); // Cập nhật trạng thái (state)
//     } catch (error) {
//       console.error("Error fetching data:", error); // Bắt lỗi nếu có vấn đề khi gọi API
//     }
//   }
  